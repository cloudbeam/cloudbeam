Document Shared

<%= "You have shared the document #{@document.name} with #{@recipient_emails.length} people:" %>
<% @recipient_emails.each do |recipient_email| %>
  <%= recipient_email %>
<% end %>

<% if @message == "" %>
  You didn't send them any message.
<% else %>
  You sent them the following message:
  <%= @message %>
<% end %>

What now? You have provided each recipient with one-time access to download this file. Once they use their unique download code, it will become invalid.
We'll let you know when file recipients downloaded the content.
Note: Cloudbeam is a temporary storage solution for the purpose of efficiently sharing content.  All uploaded files have a maximum active storage of 30 days.  Your file expires <%= @expiration %>.
View your file dashboard on Cloudbeam: <%= "https://cloud-beam.com/dashboard" %>
