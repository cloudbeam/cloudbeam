Document Shared

<%= "You have shared the document #{@document.name} to #{@recipient_emails.length} people:" %>
<% @recipient_emails.each do |recipient_email| %>
  <%= recipient_email %>
<% end %>

<% if @message == "" %>
  You didn't send them any message.
<% else %>
  You sent them the following message:
  <%= @message %>
<% end %>